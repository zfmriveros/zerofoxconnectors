**Unreleased**

* #### Overview
* Added explicit runtime dependency pins for `requests==2.32.3` and `beautifulsoup4==4.12.3`.
* Verified compatibility target for Python 3.9 on SOAR 6.x and Python 3.11 on SOAR 7.x+ keeping the minimum SOAR version at 6.1.0.
